# pymaco
python machine configurator : an application that automates the installation and configuration of machines

The purpose is similar to puppet and ansible.
`pymaco` is designed to be able to configure linux, macposx and windows systems.

